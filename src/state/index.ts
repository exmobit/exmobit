import { IContext } from './context';

const state: IContext = {
  currencyData: [],
  currencyFrom: null,
  currencyTo: null,
  updateState: () => { },
  wallet: '',
  email: '',
};

export default state;
